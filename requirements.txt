# Python 3.9. Dependencies for the theoretically analysed experiments only.
# The real-world LLM experiments need an incompatible stack (Python 3.11, torch
# 2.10); see requirements_real_world_llm.txt. The synthetic LLM experiments
# build their own conda env; see synthetic_llm_experiments/scripts/setup_env.sh.

torch==2.5.1
numpy==1.26.4
matplotlib==3.9.4
tqdm==4.67.1
pillow==11.3.0
pyyaml==6.0.2
