# Python 3.11. Dependencies for the LLM experiments only; these are the exact
# versions used for the reported runs. The theoretically analysed experiments
# need an incompatible stack (Python 3.9, torch 2.5.1); see requirements.txt.
#
# TRL 1.2.0 prints a warning that it officially supports vLLM 0.11.0-0.18.0 and
# that 0.19.1 is untested. The colocate path works correctly with 0.19.1; the
# warning can be ignored.
#
# flash-attn must be installed after torch (it compiles against it):
#   pip install torch==2.10.0
#   pip install -r requirements_llm.txt --no-build-isolation

torch==2.10.0
transformers==5.5.4
trl==1.2.0
datasets==4.8.4
accelerate==1.13.0
deepspeed==0.18.9
vllm==0.19.1
peft==0.19.1
flash-attn==2.8.3
tensorboard==2.20.0
networkx==3.6.1
numpy==2.2.6
matplotlib==3.10.8
tqdm==4.67.3
